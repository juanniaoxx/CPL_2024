#include <stdio.h>
#define N 4069

char upper(char x) {
    return x + 'a' - 'A';
}
int main() {
    int i = 0;
    char string[N];

    fgets(string, N, stdin);

    while (string[i++] != '\n') {

    }
}
