#include <stdio.h>
#include <string.h>

int main(int argc, char *argv[]) {
  if (argv[1])
    if (!strcmp(argv[1], "version"))
      fprintf(stderr, "%s", 
#include "icon.txt"
          );
  return 0;
}

